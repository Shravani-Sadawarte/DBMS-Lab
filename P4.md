PRACTICAL 4



AIM : 



1\. Display all employee names that starts with "a" and are at least 3 character in length,



-> select \* from emo where lower(first\_name) like 'a%' and length(First\_name) >= 3;



Display the name of employees who are working as clerk salesman or analyst and drawing a salary more than 3000.

select first\_name





2\. Display the employee number and name of employee working as cleark and earning the highest salary amongs clearks,



select \* from emp where  



3\. Display the name of emp who earns highest salary



select max(salary) from emp;





4.display the employee number as name of the employee working as cleark and earning highest among clearks

SELECT emp\_no, first\_name

FROM emp

WHERE job = 'CLERK'

&nbsp; AND salary = (

&nbsp;       SELECT MAX(salary)

&nbsp;       FROM emp

&nbsp;       WHERE job = 'CLERK'

&nbsp;     );



5.Display the name of clearks that have earned salary more than bob and less than that of



SELECT first\_name

FROM emp

WHERE job = 'CLERK'

&nbsp; AND salary >

&nbsp;     (SELECT salary FROM emp WHERE first\_name = 'Bob')

&nbsp; AND salary <

&nbsp;     (SELECT salary FROM emp WHERE first\_name = 'James');





6.Display the nanes of the empoyees who earn highest salary in their respective departments.



SELECT e.first\_name, d.dept\_name

FROM emp e

JOIN departments d ON e.deptno = d.deptno

WHERE e.salary = (

&nbsp;   SELECT MAX(salary)

&nbsp;   FROM emp

&nbsp;   WHERE deptno = e.deptno

);



* if the department names where also visible then the code :



SELECT e.first\_name, d.dept\_name

FROM emp e, departments d

WHERE e.deptno = d.deptno

&nbsp; AND e.salary = (

&nbsp;       SELECT MAX(salary)

&nbsp;       FROM emp

&nbsp;       WHERE deptno = e.deptno

&nbsp;     );







7\. Display those emp who are working in the dept where his manager is working in the same department where the manager is .



SELECT e.emp\_no, e.first\_name

FROM emp e, emp m

WHERE e.manager\_id = m.emp\_no

&nbsp; AND e.deptno = m.deptno;





