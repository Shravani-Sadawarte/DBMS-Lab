Practical -03



AIM: Consider the problem definition in practical 1.Write and execute SQL functions aggregates , numeric, date, string and conversions for the following:



-> Find avg salary of Instructor in CS Department.

-> Find the avg salary in each department.

-> What is the total salary of all teachers earning more than 30K?

-> List the number of students enrolled in each course.

->Display length of name of all student.

-> Display names of instructor working for past 5 yrs.

->Print a list of instructors displaying "Less Salary" if less than 60000 display 'Exact Salary' and if greater than  60000 display "More Salary"

->Retrieve distinct number of instructors from each department.

---

* To find the instructor of CS :

 

select \* from instructor where dept\_id = (select dept\_id from department where name = 'Computer Science');



* Avg salary:

 select avg(salary) as avg\_salary from instructor where dept\_id = (select dept\_id from department where name = 'Computer Science');



* using JOINS:

select avg(salary) as avg\_salary from instructor I *joins* department D on i.dept\_d =d.dept\_id where d.name= 'Computer Science';



// ERROR: ERROR at line 1:

ORA-00933: SQL command not properly ended



select avg(I.salary) as avg\_salary from instructor I join department D on i.dept\_id = d.dept\_id where  d.name= 'Computer Science';





* Avg in each Dept:

SELECT d.name AS department\_name, AVG(i.salary) AS avg\_salary FROM instructor i JOIN department d ON i.dept\_id = d.dept\_id GROUP BY d.name;







* TABLE CREATION:





CREATE TABLE STUDENT (

    student\_id NUMBER(38) PRIMARY KEY,

    name       VARCHAR2(50) NOT NULL,

    tot\_cred   NUMBER(3)

);



INSERT INTO STUDENT VALUES (101, 'Arjun', 20);

INSERT INTO STUDENT VALUES (102, 'Meera', 22);

INSERT INTO STUDENT VALUES (106, 'Rahul', 18);

INSERT INTO STUDENT VALUES (107, 'Sneha', 19);

INSERT INTO STUDENT VALUES (108, 'Kiran', 21);



CREATE TABLE ENROLLMENT (

    enrollment\_id INT PRIMARY KEY,

    student\_id INT NOT NULL,

    section\_id INT NOT NULL,

    enrollment\_date DATE,

    status VARCHAR(20) DEFAULT 'ENROLLED',

    FOREIGN KEY (student\_id) REFERENCES STUDENT(student\_id),

    FOREIGN KEY (section\_id) REFERENCES SECTION(section\_id)

);



INSERT INTO ENROLLMENT VALUES (1, 101, 1, DATE '2024-07-01', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (2, 102, 1, DATE '2024-07-01', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (3, 101, 2, DATE '2024-07-02', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (4, 102, 2, DATE '2024-07-02', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (5, 106, 3, DATE '2024-07-03', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (6, 107, 4, DATE '2024-07-03', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (7, 108, 5, DATE '2024-07-04', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (8, 101, 6, DATE '2024-07-04', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (9, 102, 7, DATE '2024-07-05', 'ENROLLED');

INSERT INTO ENROLLMENT VALUES (10,106, 8, DATE '2024-07-05', 'ENROLLED');





* List of students enrolled in each course:



SELECT C.course\_id,

       C.title,

       COUNT(E.student\_id) AS enrolled\_students

FROM course C

LEFT JOIN section S

ON C.course\_id = S.course\_id

LEFT JOIN enrollment E

ON S.section\_id = E.section\_id

GROUP BY C.course\_id, C.title

ORDER BY C.course\_id;





* To select a particular row:

SELECT name, LENGTH(name) AS namelength FROM student;



* names of instructor working for past 5 yrs.:



ALTER TABLE instructor

ADD hire\_date DATE;



UPDATE instructor

SET hire\_date = DATE '2018-06-15'

WHERE instructor\_id = 1;



UPDATE instructor

SET hire\_date = DATE '2021-03-10'

WHERE instructor\_id = 2;





SELECT name

FROM instructor

WHERE HireDate <= ADD\_MONTHS(SYSDATE, -60);





* SELECT I.NAME || '-'|| D.NAME AS NAME\_VS\_DPT FROM INSTRUCTOR I JOIN DEPARTMENT D ON I.DEPT\_ID=D.DEPT\_ID ;



* Retrieve distinct number of instructors from each department :



SELECT d.name AS department\_name, COUNT(DISTINCT i.instructor\_id) AS total\_instructors

FROM instructor i

JOIN department d

ON i.dept\_id = d.dept\_id

GROUP BY d.name;

&nbsp;























